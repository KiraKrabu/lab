#include <iostream>
using namespace std;

void simple(int a) {
 int count;
  count = 0;
   for (int i = 1; i < a; i++) {
     if (a % i == 0) {
       count += 1;
      }
   else count += 0;
   }
 if (count > 1)
 cout << "сложное" << endl;
 else
 cout << "простое" << endl;
}
int main() {
 int a, b;
 cout << "Введите числа: ";
 cin >> a >> b;
 simple(a);
 simple(b);
}
